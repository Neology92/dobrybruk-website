import React from 'react';
import styled from 'styled-components';

const Map = () => {
  return <MapWrapper />;
};

const MapWrapper = styled.div`
  background: red;
  width: 60%;
`;

export default Map;
