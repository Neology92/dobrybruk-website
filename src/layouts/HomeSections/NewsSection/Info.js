import styled from 'styled-components';

const Info = styled.p`
  color: ${({ theme }) => theme.color.greyL1};
  font-size: 1.5rem;

  text-align: center;
  padding: 30px 0 0 0;
  margin: 0;
  font-weight: 400;
`;

export default Info;
